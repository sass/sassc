@mixin foo($x, $y, $zs...) {
  stuff: $x;
  more-stuff: $y;
  even-more-stuff: $zs;
  //$combined1: join(1 2 3, $zs);
  //$combined2: join($zs, 1 2 3);
  blah: type-of($zs);
  flah: nth($zs, 1);
  //c1: type-of($combined1), $combined1;
  //c2: type-of($combined2), $combined2;
}

@mixin bar($x, $y, $z) {
  stuff: $x, $y, $z;
  blah: type-of($z);
}

$rest: c d e;

div {
  @include foo(a, b, c d e...);
  ///* hey */
  //@include bar(1, 2, 3 4...);
  //blug: join(1 2 3, (c, d, e));
}